# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

PISM (Private Image Storage Management) — a Visual Studio / .NET project (inferred from `.gitignore`). The repository is in early setup; no source files or build configuration have been added yet.

## Getting Started

Once project files are added, update this file with:
- Solution/project file locations (`.sln`, `.csproj`)
- Build commands (e.g., `dotnet build`, MSBuild invocations)
- How to run tests (e.g., `dotnet test`)
- Any environment setup required (connection strings, local config)
