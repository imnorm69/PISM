# Copy this file to .env and fill in values for local overrides.
# .env is gitignored and never committed.

# PostgreSQL connection string for PISM.Web / PISM.Worker
ConnectionStrings__Default=Host=localhost;Database=pism;Username=pism;Password=

# Anthropic Claude API key (required for AI-assisted features)
Anthropic__ApiKey=
